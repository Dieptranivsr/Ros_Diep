

    >>> cd Documents/
    >>> mkdir build-system
    >>> cd build-system
exp:

    >>> nano hello-diep.cpp
    >>> g++ hello-diep.cpp -o hello-diep
    >>> ls
result:

    hello-diep   hello-diep.cpp
next:
    nano Makefile
    
